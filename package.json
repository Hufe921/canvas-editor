{
  "version": "0.6.2",
  "scripts": {
    "dev": "vite",
    "lib": "tsc && vite build --mode lib",
    "build": "tsc && vite build --mode app",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^16.11.12",
    "typescript": "^4.3.2",
    "vite": "^2.4.2"
  }
}