import { IHintOption } from '../../interface/Hint'

export const defaultHintOption: Readonly<Required<IHintOption>> = {
  disabled: true,
  backgroundColor: '#fff',
  color: '#000000',
  fontSize: 12,
  maxWidth: 280
}
