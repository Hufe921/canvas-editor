import { IMagnifierOption } from '../../interface/Magnifier'

export const defaultMagnifierOption: Readonly<Required<IMagnifierOption>> = {
  disabled: true,
  size: 120,
  zoom: 2,
  borderColor: '#efefef'
}
