export enum TextDirection {
  LTR = 'ltr',
  RTL = 'rtl',
  AUTO = 'auto'
}

export enum TextEngineMode {
  LEGACY = 'legacy',
  HARFBUZZ = 'harfbuzz'
}

export enum CaretMovement {
  VISUAL = 'visual',
  LOGICAL = 'logical'
}

export enum DeleteMovement {
  VISUAL = 'visual',
  LOGICAL = 'logical'
}
