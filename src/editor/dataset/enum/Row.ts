export enum RowFlex {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
  ALIGNMENT = 'alignment',
  JUSTIFY = 'justify',
  /** Logical start: left in LTR paragraphs, right in RTL */
  START = 'start',
  /** Logical end: right in LTR paragraphs, left in RTL */
  END = 'end'
}
